import {
    Link
} from "react-router-dom";

function Stream() {
    return (
        <div className="App">
            stream
            <Link to="/Home">Home</Link>
        </div>
    );
}

export default Stream;
